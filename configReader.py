#!/usr/bin/python


# Accesses the configuration files, scrapes data from the files, and returns values such.

# *******************************************
# CC-BY-SA Winter Guerra (XtremD), June 2013.
# *******************************************

class ConfigReader():
    
    # Constructor
    def __init__(self, configURL):
        # Open the config file
        
        # Sanitize the config file
        
        pass
    
    # Returns all the lines of a config file (whitespace and all)
    def openConfig(fileURL):
    
        config_Lines = {}
    
        return config_Lines
        
    # Takes a dataset of all the lines of a config and removes comments and empty space
    def cleanConfigDataset():
        
        config_Dictionary = {}
        
        return config_Dictionary
        
    def getValue(key):
        
        config_Value
        
        return config_Value
        
    def _ifKeyDefined(key):
        
        is_Key_Defined
        
        return is_Key_Defined